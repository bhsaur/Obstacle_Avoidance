# PLAN-EVAL-FILES — evaluation file guide

Codex; baseline c9c0de1. User clarified that the plan should describe actual
evaluation files. Added section 9A to PROJECT_PLAN.md: reusable scripts and
limitations, run-file meanings, existing artifact locations, offline plotting
commands, and explicit separation from the still-unbuilt six-arm aggregator.
Credits existing tooling rather than presenting all scripts as newly created.

Validation: inspected script CLI/accepted controller variants; checked all
relative file links in the plan resolve; git diff --check passes. No runtime
code changes, new tests, plotting executions or simulator launches. Task state
review; next reader can use the documented commands on supplied recordings.
